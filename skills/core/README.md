# Core

The core layer handles library entry and routing, not product decisions or
implementation. Find the router in the [canonical index](../INDEX.md).

A direct repository request starts at the root [AGENTS.md](../../AGENTS.md).
The router selects the smallest useful route and loads the next skill only.
It is optional when a specialist is already selected and its inputs are clear.
